/**
 * Tab navigator (Track 1) — the two persistent screens: Home (notes library)
 * and Settings. Uses the shared `AppTabs` component (native + web variants).
 */
import AppTabs from '@/components/app-tabs';

export default function TabsLayout() {
  return <AppTabs />;
}
